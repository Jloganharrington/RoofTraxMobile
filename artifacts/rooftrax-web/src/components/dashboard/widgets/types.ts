// Shared prop type for all dashboard widget components.
// Widgets receive no props — they are self-contained and fetch their own data.
// Size and title are layout concerns owned by DashboardGrid/WidgetCard.
export interface DashboardWidgetProps {
  // intentionally empty
}
