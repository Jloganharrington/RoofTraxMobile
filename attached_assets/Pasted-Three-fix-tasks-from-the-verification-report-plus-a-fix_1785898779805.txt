Three fix tasks from the verification report, plus a fixture task. Tasks 1 and 2 can run in parallel; task 3 after both; task 4 (Track D) launches per its existing prompt after task 1 merges.

=== TASK 1 — Track B completion (the F-5 implementation is a stub; build the runtime) ===
a. KEY HYGIENE FIRST: the standards importer conflated key and title — entry_key contains the full title line (e.g. "STD-WTR-01 — ANSI/IICRC S500 (2021, 5th Edition)"). Migrate entry_key to the short key (text before the first " — "), store the remainder in the title column, for ALL entries and all version rows. Fix the importer to parse EntryKey and Title as separate fields going forward. Then set human_entered_provisions_only = true on entry_key IN ('STD-WTR-01','STD-WTR-02') — exact match, post-migration.
b. BUILD THE FILTER: in section generation, before prompt assembly, partition referenced standards entries: entries with humanEnteredProvisionsOnly=true are EXCLUDED from prompt context entirely (no citation text, no locator, no title beyond the entry key); the worker injects {{IICRC_CITATION_PLACEHOLDER:entryKey}} tokens in the generated section where the citation belongs. The route must fetch the flag from the DB row and forward it to generateSectionContent.
c. REVIEWER COMPLETION: placeholder tokens render in the section review UI as a required fill-in (citation + locator from the licensed copy); a section containing an unfilled placeholder cannot be approved (lint gate, not crash).
d. TEST: interior-water claim → assembled prompt payload asserts zero occurrences of 'S500', 'S520', 'IICRC'; generated output contains the placeholder; approval blocked until filled; compile succeeds after.

=== TASK 2 — Track C three-caption structure ===
Confirmed comparison pairs must produce the Class 6 structure: ONE set caption for the pair + one caption per photo stating what that photo contributes. Implement at badge finalization: a comparison pair is one exhibit unit (stacked vertical render, one badge covering the set or per-photo badges per current map design — follow the existing badge model, do not redesign it) with a setCaption field alongside the two photo captions. Caption generator branches per comparison type (recency / cause_differentiation / covered_vs_unrelated) using the established patterns. Compiled render: set caption + stacked photos + per-photo captions, never split across pages. Test: a confirmed cause_differentiation pair produces exactly three caption strings in the correct structure.

=== TASK 3 — Virginia acceptance fixture (after 1+2 merge) ===
Create a seed script (scripts/seed-acceptance-claim.ts) that builds ONE fully-progressed Virginia claim by exercising real code paths — call the actual APIs/services, do not insert rows directly except where the field app would (simulate the outbox patch payloads):
- Wind-and-hail claim, Reston VA address; attested forensic field record including: facet damage with condition codes, edge-assembly determinations (using not_observed), test-square photos AND general-weathering photos (so the cause_differentiation slot derives), product ID = identified with a discontinued library candidate, RAP record with selection confirmations (damaged_target mode) and one delamination outcome, interior_scope_present with a traced path, estimate with 3 line items.
- Then run the acceptance walk end-to-end: field data review → curation (confirm all slots incl. the cause_differentiation pair) → generate 5 independent sections in parallel → approve with hard-gate confirmations (causationReviewConfirmed, comparison confirms, RAP mode check) → Summary → Closing → lock all → compile → attest (assert new signed blob, snapshot carries reportAttestationId + standardsCited, review blob hash-unchanged) → deliver (assert 422 before attest if tested out of order, success after). For delivery without SMTP, assert the gate passes and the delivery event writes; actual email send may be mocked.
- Output a step-by-step pass/fail log. This script is the permanent acceptance test — it must be re-runnable (fresh claim each run).

=== TASK 4 — Track D (supplement flow) ===
Launch the existing Track_D_Supplement_Flow prompt unchanged. After Task 3's fixture exists, extend its test to issue one supplement against the fixture's delivered package.