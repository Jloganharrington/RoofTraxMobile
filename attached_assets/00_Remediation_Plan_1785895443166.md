# Remediation Plan — Post-Audit Fix Sequence
Based on audit FIX items F-4 through F-10 + verification of the earlier six-fix remediation batch (F-5/F-6/F-8 indicate it never ran or partially ran).

## Priority logic
The first end-to-end deliverable package is the goal. Anything that makes a compiled/signed package untrustworthy fixes first; compliance data fixes are cheap and immediate; quality and cleanup follow; the supplement rebuild is larger and depends on Track A's blob machinery.

## Parallel track map (Replit Pro — run simultaneously)
Tracks are file-domain-disjoint to avoid agent collisions. Each prompt declares its scope and forbids edits outside it.

| Track | Priority | Covers | Domain | Run |
|---|---|---|---|---|
| A — Compile & Attest Integrity | P0 | F-6 (A–M badge scheme), F-8 (post-attest recompile) | proofPackageTemplate, compile/attest routes, snapshot | NOW |
| B — Compliance & Vocabulary | P0 | F-5 (IICRC flags + worker exclusion), F-7 (eave vocabulary) | standards data, generation worker prompt assembly, label vocab | NOW (parallel with A) |
| C — Curation Manifest Completion | P1 | F-9 (cause_differentiation) + Fix-4 verification | curation manifest, comparison enum, caption generator | NOW (parallel with A/B) |
| E — Route & Surface Cleanup | P1 | F-10 (/inspections/:id/summary) + route inventory refresh | routes, docs | NOW (parallel) |
| D — Supplement Flow Rebuild | P2 | F-4 (supplements as separate attested documents) | supplement routes/UI, blob versioning consumers | AFTER Track A merges — depends on new-blob-version machinery |

## Sequencing rules
1. A, B, C, E launch simultaneously. D launches only after A's changes merge — D consumes the attest→recompile blob-version machinery A creates.
2. Before launching ANY track: check Replit history for whether the earlier "six-fix post-build remediation" prompt ran. If it ran partially, note which fixes applied — Tracks A/B/C verify-then-fix rather than blind-fix.
3. After all tracks merge: run the full test suite once, then re-run the audit's Section 5 (compile/attest/deliver) checkpoints as the acceptance gate.
4. Known-open items NOT in these tracks (pending the rest of the audit output — F-1..F-3, BUILD list, WIRE list, end-to-end answer): field-app event wiring for pipeline auto-advance, field-app backlog items, detriment condition-code taxonomy expansion, tenant onboarding checklist. Reserve capacity; do not let agents wander into these.

## Acceptance for the whole batch
- One test claim travels: attested record → curation (incl. a cause_differentiation pair) → parallel generation → all locked → compile → attest (new signed blob, rendered Variant B, snapshot carries attestation) → deliver (gated on signed blob).
- grep finds zero A–M badge remnants, zero "Absent" eave labels in render vocabulary.
- STD-WTR-01/02 show humanEnteredProvisionsOnly=true and their text is absent from any assembled prompt payload.
- A supplement can be issued as a separate, separately-attested, badge-appending document (Track D).
